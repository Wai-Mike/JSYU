<IfModule mod_rewrite.c>
    RewriteEngine On

    # Hostinger serves from public_html; Laravel's front controller is in /public.
    # Keep root clean and forward everything into /public.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
